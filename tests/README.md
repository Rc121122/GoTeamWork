# Test Fixtures

This directory stores reusable JSON templates that integration-style HTTP handler tests load and fill with runtime values. Keeping them here makes it easy to add new routes or tweak payloads without touching the test logic.
